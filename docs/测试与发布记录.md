# 个人工作台：测试与发布记录

## 当前线上版本

- 线上地址：https://personal-workbench-visuals.yizi-huang307.chatgpt.site/
- Sites 项目 ID：`appgprj_6a6c36e62bf8819187bf60c465395acd`
- 最近确认版本：v32
- 最近源码提交：`0885f915ba820463fe2f78652a5fe2bb09372187`
- 发布日期：2026-08-01

## 当前自动测试

源码测试位于 `site/tests/`：

- `records.test.ts`：记录排序、搜索、分类删除迁移、异常数据、1000 条记录、图片位置、AI 状态隔离等。
- `information.test.ts`：链接安全、标题生成、搜索、异常数据、文件容量、1000 条资料、历史数据迁移等。
- `site-metadata.test.ts`：网页名称和图标识别、内网地址拦截、恶意重定向拦截。
- `rendered-html.test.mjs`：页面结构、菜单、搜索框、AI 操作位置和接口失败状态。

## v32 验证结果

- 逻辑测试：22 项通过。
- 页面结构测试：7 项通过。
- 静态检查：无错误，保留 4 条已有警告。
- 生产构建：通过。
- 真实页面验证：常用链接名称识别、编辑入口、完整首行标题已验证。
- 文件保存机制：已从 localStorage 切换到 IndexedDB，解决约 5.7MB 文件导致的配额失败。

## 发布前固定检查

1. 运行全部自动测试。
2. 运行静态检查和生产构建。
3. 在真实页面验证桌面端和手机端核心链路。
4. 检查刷新恢复、异常存储、长列表、菜单退出、弹层退出和焦点。
5. 将本轮确认规则同步到 `docs/`。
6. 将源码、版本号、提交号和线上地址更新到本文件。

## 2026-08-02【链接／资料】拆分模块验证

- 测试用例：见 `docs/链接与资料模块测试用例.md`，覆盖链接、分组、资料分类、详情编辑、存储、响应式和极端场景。
- 功能规则测试：46 项通过，其中信息模块 33 项，包含分类排序、组内排序、跨组移动、空分类、置顶层级、1000 条数据和损坏顺序恢复。
- 页面结构测试：20 项通过。
- 静态检查：0 错误；5 条既有非阻断警告。
- 生产构建及发布产物校验：通过。
- 桌面真实页面：分类上下移动、资料跨分类移动并恢复、链接搜索高亮通过。
- 390px 手机页面：页面无横向溢出；固定宽度看板可横向滑动；列头新增按钮及移动替代入口可见。
- 浏览器控制台：无错误。
